specVersion: "v1beta"
serviceId: "firecard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firecard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
